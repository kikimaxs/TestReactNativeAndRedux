import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flex: 1, alignSelf: 'stretch', width: '100%' },
  input: {
    height: 40,
    backgroundColor: '#f0f0f0',
    borderRadius: 8,
    paddingHorizontal: 12,
    width: '100%',
  },
});

export default styles;